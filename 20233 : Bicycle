#include<iostream>
using namespace std;

int main()
{
	int a, x, y, b, T;
	int result_1, result_2;

	cin >> a;
	cin >> x;
	cin >> b;
	cin >> y;
	cin >> T;

	if (T>= 30) {
		cout << a + 21 * (x * (T - 30)) << " ";
	}
	else {
		cout << a << " ";
	}

	if (T - 45 >= 0) {
		cout << b + 21 * (y * (T - 45));
	}
	else {
		cout << b;
	}

	return 0;
}
