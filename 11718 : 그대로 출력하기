#include<iostream>
#include<array>
#include<string>
#include<vector>
using namespace std;

int main() {
    string str;
    vector<string> v;

    while (cin.eof() != true) {
        getline(cin, str);
        v.push_back(str);
    }

    for (auto& value : v) {
        cout << value << endl;
    }

    return 0;
}
