#include<iostream>

using namespace::std;

int fibonacciNum(int n);

int main() {
	int n;

	cin >> n;
	cout << fibonacciNum(n);

	return 0;
}

int fibonacciNum(int n) {
	if (n == 1) return 1;
	else if (n == 0) return 0;
	else return fibonacciNum(n-1) + fibonacciNum(n-2);
}
